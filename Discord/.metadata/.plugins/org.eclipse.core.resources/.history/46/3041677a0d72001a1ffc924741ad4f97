package Commands;

import com.sedmelluq.discord.lavaplayer.track.AudioTrack;
import com.sedmelluq.discord.lavaplayer.track.AudioTrackInfo;

import PackageCommands.InterfaceCommand;
import PackageMusic.ClassMusic;
import net.dv8tion.jda.api.EmbedBuilder;
import net.dv8tion.jda.api.entities.Guild;
import net.dv8tion.jda.api.events.message.MessageReceivedEvent;

public class cmdNow implements InterfaceCommand{
	private Guild guild;
	@Override
	public boolean called(String[] args, MessageReceivedEvent event) {

		return false;
	}

	@Override
	public void action(String[] args, MessageReceivedEvent event) {
		//if (ClassMusic.isIdle(guild)) return;
		
		AudioTrack track = ClassMusic.getPlayer(guild).getPlayingTrack();
		AudioTrackInfo info = track.getInfo();
		
		event.getTextChannel().sendMessage(
				new EmbedBuilder()
					.setDescription("**Tocando agora: **")
					.addField("Title", info.title, false)
					.addField("Duration", "`[ " + ClassMusic.getTimestamp(track.getPosition()) + "/ " + ClassMusic.getTimestamp(track.getDuration()) + " ]`", false)
					.addField("Ator", info.author, false)
					.build()
				).queue();
	}

	@Override
	public void executed(boolean sucess, MessageReceivedEvent event) {

		
	}

	@Override
	public String help() {

		return null;
	}
	

}
